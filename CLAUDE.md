# Playtest Instructions For Claude

When the user asks for a playtest, manual UX test, target-user simulation, or "use this app like a real user", follow the Playtest workflow in `skills/playtest/SKILL.md`.

If Claude Code skills are available, load the `playtest` skill. If skills are not available, use `AGENTS.md` as the fallback workflow.

Key rule: write `docs/playtest/<date>/test-plan.md`, then stop for human scope selection before using the product.
